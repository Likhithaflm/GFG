class Solution{
    public:
    string longest(string names[], int n)
    {
        string a=names[0];
        for(int i=0; i<n; i++){
            if(names[i].size()>a.size())
            {
                a=names[i];
            }
        }
        return a;
        
    }
